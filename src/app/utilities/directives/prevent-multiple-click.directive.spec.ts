import { PreventMultipleClickDirective } from './prevent-multiple-click.directive';

describe('PreventMultipleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventMultipleClickDirective();
    expect(directive).toBeTruthy();
  });
});
